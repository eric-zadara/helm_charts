# KServe CRDs Chart Configuration
# Source: https://github.com/kserve/kserve/releases/download/v0.16.0/kserve.yaml

# -- Skip CRD installation if they already exist
# Set to true to prevent overwriting existing CRDs
skipIfExists: false

# -- Retain CRDs when helm uninstall is run
# IMPORTANT: Setting to false will delete all InferenceService and ServingRuntime
# resources when the chart is uninstalled (Kubernetes garbage collection)
keepOnUninstall: true
