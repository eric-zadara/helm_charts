apiVersion: v2
name: inference-stack
description: Model serving and routing layer for LLM inference platform
type: application
version: 0.2.0
appVersion: "1.0.0"
annotations:
  testing/tier: "3"
  testing/prerequisites: "inference-operators,inference-infrastructure"
