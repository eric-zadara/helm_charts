# CI test values for inference-operators
# Minimal overlay -- production defaults are correct for CI.
# All operators and CRD charts are enabled by default in values.yaml.
#
# This file is intentionally nearly empty because production defaults
# are appropriate for CI testing. The chart-testing (ct) convention
# requires a ci/default-values.yaml file to exist.
