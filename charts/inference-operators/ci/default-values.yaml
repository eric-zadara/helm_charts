# CI test values - all operators and CRD charts enabled (default configuration)
# Used by chart-testing (ct lint) to validate the umbrella chart

# CNPG operator - enabled by default
cloudnative-pg:
  enabled: true

gateway-api:
  enabled: true

knative-serving-crds:
  enabled: true

kserve-crds:
  enabled: true

inference-extension-crds:
  enabled: true
